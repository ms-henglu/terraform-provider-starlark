# Basic Math Example

This example demonstrates how to use the `provider::starlark::eval` function to perform basic arithmetic operations.

## Usage

1. Initialize Terraform:
   ```bash
   terraform init
   ```

2. Apply the configuration:
   ```bash
   terraform apply
   ```

3. Check the outputs for the results.
